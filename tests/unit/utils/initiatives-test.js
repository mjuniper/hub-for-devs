import initiatives from 'hub-for-devs/utils/initiatives';
import { module, test } from 'qunit';

module('Unit | Utility | initiatives', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = initiatives();
    assert.ok(result);
  });
});
